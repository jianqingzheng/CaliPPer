# pip fallback (use environment.yml with conda for the recommended setup).
#
# The scientific stack is pinned to the exact versions the committed figure
# reference values were generated with; verify.sh checks reproduced values
# against them at 1e-10, so bit-exact reproduction needs this exact stack.
#
# IMPORTANT — numpy MUST stay <2.0: numpy>=2 changed np.histogram/np.digitize
# bin-edge behaviour (shifts CaliPPer's calibration bins → ~1e-2 drift in Fig 6
# cal_ap/cal_aucroc) and removed np.trapz (used by the Fig 6 panel code).
#
# Recommended: python 3.10.

numpy==1.26.4
scipy==1.11.4
pandas==2.1.4
scikit-learn==1.3.2
matplotlib==3.8.2
seaborn>=0.12
openpyxl==3.1.5
Levenshtein>=0.21
# Smith-Waterman BLOSUM62 alignment — REQUIRED by Fig 6 (BigMHC/deepAntigen s2dd)
parasail==1.3.4
jupyter>=1.0
ipykernel>=6.20

# Dev (optional)
pytest>=7.0
pytest-cov>=4.0
black>=23.0
ruff>=0.1
pre-commit>=3.0
nbstripout>=0.6
